// START - Edit these values
// ------------------------------------------

// launchAnimations.js
// ------------------------------------------

// Default video path
export const defaultLaunchFileName = '[PATH_TO_FILE]';

// Use a random video
// If this is `true` then the default file above is ignored
export const useRandomVideo = true

// Whether or not to disable the wheel image when the animation plays
export const disableWheel = true;

// Whether or not to disable the messaging when the animation plays
export const disableMessaging = true;

// Check to `true` if you want to output table animation details to the log
export const debug = false;

// launchLoadingVideos.js
// ------------------------------------------
// Uses defaultLaunchFileName, disableWheel and disableMessaging

// launchLoadingVideos.js
// ------------------------------------------
// Uses defaultLaunchFileName, disableWheel and disableMessaging

// randomLoadingAnimation.js
// ------------------------------------------
// PREP: Place any number of loading videos in you `Videos` folder
// https://www.videezy.com/free-video/abstract-loop - check here for videos
// Uses disableWheel and disableMessaging

// ------------------------------------------
// END - Edit these values
